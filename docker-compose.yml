services:
  signal-watcher:
    build: .
    container_name: signal-watcher
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./.state:/app/.state
